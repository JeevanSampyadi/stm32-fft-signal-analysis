# stm32-fft-signal-analysis
Implementation of FFT-based signal analysis using CMSIS-DSP on STM32 NUCLEO-G071RB. The project processes a provided signal array, computes the magnitude spectrum, detects the dominant frequency, and outputs results via UART with optional LED feedback.
